"use strict";
/**
 * Copyright 2023 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SigningAlgorithm = void 0;
exports.createTestSigner = createTestSigner;
const promises_1 = require("node:fs/promises");
const node_path_1 = require("node:path");
const pkg_dir_1 = __importDefault(require("pkg-dir"));
var SigningAlgorithm;
(function (SigningAlgorithm) {
    // ECDSA with SHA-256
    SigningAlgorithm["ES256"] = "es256";
    // ECDSA with SHA-384
    SigningAlgorithm["ES384"] = "es384";
    // ECDSA with SHA-512
    SigningAlgorithm["ES512"] = "es512";
    // RSASSA-PSS using SHA-256 and MGF1 with SHA-256
    SigningAlgorithm["PS256"] = "ps256";
    // RSASSA-PSS using SHA-384 and MGF1 with SHA-384
    SigningAlgorithm["PS384"] = "ps384";
    // RSASSA-PSS using SHA-512 and MGF1 with SHA-512
    SigningAlgorithm["PS512"] = "ps512";
    // Edwards-Curve DSA (Ed25519 instance only)
    SigningAlgorithm["Ed25519"] = "ed25519";
})(SigningAlgorithm || (exports.SigningAlgorithm = SigningAlgorithm = {}));
const packageRoot = pkg_dir_1.default.sync(__dirname) ?? '';
const defaultTestSignerOptions = {
    certificatePath: (0, node_path_1.resolve)(packageRoot, 'tests/fixtures/certs/es256.pub'),
    privateKeyPath: (0, node_path_1.resolve)(packageRoot, 'tests/fixtures/certs/es256.pem'),
};
async function createTestSigner({ certificatePath, privateKeyPath, } = defaultTestSignerOptions) {
    const [certificate, privateKey] = await Promise.all([
        (0, promises_1.readFile)((0, node_path_1.resolve)(process.cwd(), certificatePath)),
        (0, promises_1.readFile)((0, node_path_1.resolve)(process.cwd(), privateKeyPath)),
    ]);
    return {
        type: 'local',
        certificate,
        privateKey,
        algorithm: SigningAlgorithm.ES256,
        tsaUrl: 'http://timestamp.digicert.com',
    };
}
